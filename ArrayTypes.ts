const users : [] = [] // empty array
// users.push(1);  -- doesn't work bcz it is empty array.

const UserList : string[] = []
UserList.push('bubu')
UserList.push('dudu')

const age : number[] = [1,3,45,45]

const bools : boolean[] = []       // -- this and the below are same
const bools1 : Array<boolean> = []

type Point2 = {
    x: number,
    y : number
}

const coords : Point[] = []
coords.push({x:23,y:56})

//  MULTI DIMENSIONAL ARRAY

const board : string[][] = [["s",'a',"i"],["s",'a',"i"],["s",'a',"i"]]



 